// Generated by https://github.com/foxglove/foxglove-sdk

include "PackedElementField.fbs";
include "Pose.fbs";
include "Time.fbs";

namespace foxglove;

/// A collection of N-dimensional points, which may contain additional fields with information like normals, intensity, etc.
table PointCloud2 {
  /// Timestamp of point cloud
  timestamp:Time (id: 0);

  /// Frame of reference
  frame_id:string (id: 1);

  /// The origin of the point cloud relative to the frame of reference
  pose:foxglove.Pose (id: 2);

  /// Number of bytes between points in the `data`
  point_stride:uint32 (id: 3);

  /// Fields in `data`.
  fields:[foxglove.PackedElementField] (id: 4);

  /// Point data, interpreted using `fields`
  data:[uint8] (id: 5);

  /// Number of bytes between rows in the `data`. If the point cloud is unordered, this should be set to `point_stride * num_points`.
  row_step:uint32 (id: 6);

  /// Whether the point data is big-endian. If false, the data is little-endian.
  is_bigendian:bool (id: 7);

  /// Whether the point cloud is dense (i.e., all points are valid). If false, there may be invalid points in the data, which should be ignored when processing. This field is not used by Foxglove Studio, but may be useful for applications that need to distinguish between valid and invalid points.
  is_dense:bool (id: 8);

  /// Height of the point cloud. For unordered point clouds, this should be set to 1.
  height:uint32 (id: 9);

  /// Width of the point cloud. For unordered point clouds, this should be set to the number of points in the cloud. For ordered point clouds, this should be set to the width of the point cloud, and `height` should be set to the height of the point cloud.
  width:uint32 (id: 10);
}

root_type PointCloud2;
